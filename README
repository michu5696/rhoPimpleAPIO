
Just compile as usual like any other OpenFOAM solver by typing "wmake" inside the main directory rhoPimpleAPIO/.

You can change the binary name by modifying it on Make/files. 
default: rhoPimpleAPIO 


